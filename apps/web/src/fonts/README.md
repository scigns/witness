# Witness application fonts

The authenticated application self-hosts the same SIL Open Font License assets
as the commercial site: Newsreader for editorial headings, IBM Plex Sans for
interface/body text, and IBM Plex Mono for machine-generated facts. The font
files and licence copies are kept inside this build surface so the application
does not depend on a font CDN or on another deployable application's runtime.
