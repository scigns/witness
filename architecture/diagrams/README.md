# Standalone Diagrams

**Owner:** Principal Architect
**Status:** Active

Mermaid diagrams not embedded in a document.

**Mermaid only, in-repo.** No binary formats, no external tooling — a diagram that drifts from the
prose beside it is worse than no diagram. Prefer embedding in the document it explains; this
directory is for diagrams referenced from several places.
