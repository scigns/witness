{
  "name": "@witness/contracts",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "description": "API and event contracts — OpenAPI, GraphQL SDL, AsyncAPI. Apache-2.0 so integrators face no copyleft obligation (ADR-0002).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  }
}
